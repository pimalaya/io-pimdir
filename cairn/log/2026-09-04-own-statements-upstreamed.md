---
cairn: log
change: own-statements-upstreamed
date: 2026-09-04
---

# The crate's own statements, upstreamed or justified

Of the sixteen seam statements sql.rs carried beside the canonical ones, thirteen served rules the standard states and are canonical now (the diff's summary and address loads, the binding drops, the retained row, the origin, the move exception, the queued add's collision check, two bindings reads); two were unreachable deletes of a mail or contact summary and went; one, the queue row read, is unnecessary since `cancel_action` returns the pin and `park_action` counts the attempt. `OWN` holds the operator tool's ten diagnostics, and spec-fidelity says that is all it may hold.
