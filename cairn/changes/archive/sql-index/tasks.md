---
cairn: tasks
change: sql-index
---

# Tasks

- [x] `sql`: the `ALL` index over every statement constant.
- [x] `sql`: the source-derived coverage test, so a new statement that misses the
      index fails the suite.
- [x] Fold the delta into `cairn/spec/store.md` and write the log entry.
