---
cairn: tasks
change: single-writer-guard
---

# Tasks

- [x] `write` uses `transaction_with_behavior(Immediate)`.
- [x] `PimdirError::Busy` variant + `busy_or_sql` mapping at begin and commit.
- [x] Fold `delta.md` into `cairn/spec/store.md`; add `cairn/log` entry; mark
      change `landed`.
- [x] `cargo test` / `fmt` / `clippy` clean.
