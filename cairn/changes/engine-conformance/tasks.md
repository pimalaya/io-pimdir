---
cairn: tasks
change: engine-conformance
---

- [x] `change.rs`: the flag count in decimal ASCII, every vector key reproduced.
- [x] `sync.rs`: one `Pending` map the flag axis updates; `Auto` delete policy; a re-listed probe rewrites nothing; `rejected` counts pushed handles only; `Join` into `sync/join.rs`, tests into `sync/tests.rs`.
- [x] `upgrade.rs`: a fetched hint held by a pending create lands it, `Superseded` drop and rebased binding; tests into `upgrade/tests.rs`.
- [x] `rekey.rs`: a differing revision carries a pull or a `Conflict`, never a base at it; `FETCH_CHUNK`; tests into `rekey/tests.rs`.
- [x] `hub.rs`: a tombstone's known flags ride along; a body-less item is no divergence for a bound source; the resolver answers a tombstone's destination; tests into `hub/tests.rs`.
- [x] `mutate.rs`: an `Edit` on a `Created` placement clears its origin; tests into `mutate/tests.rs`.
- [x] `placement.rs`: a tombstone's origin is its destination.
- [x] Coroutines: present-tense states, `UnexpectedArg` after completion for every resume.
- [x] `tests/vectors_sync.rs`: every push field, the `mutate` verb, one line per case, the exact skip message.
- [x] `tests/engine/`: no `let _ =` around a verb; an erroring sync is not reported; raced items assert edit-beats-delete; server equals local; `PimdirRekey` in the arg-sequence property; cold moves over a relocating and a rejecting remote.
- [x] sync.md, upgrade.md, rekey.md, mutate.md, hub.md, coroutine.md folded; the log entry written.
